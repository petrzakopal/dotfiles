return { {
    dir = "~/.config/nvim/zakopal_theme",
    name = "zakopal_theme", -- Optional, useful for requiring it easily
    dev = true,             -- Optional, treats it as a development plugin
    config = function()
        --vim.cmd("colorscheme zakopal_theme")
    end
} }
